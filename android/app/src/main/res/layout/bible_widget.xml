<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="@drawable/widget_background">

    <!-- Verso en la parte superior (varias líneas) -->
    <TextView
        android:id="@+id/widget_verse_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_above="@+id/widget_reference"
        android:layout_marginBottom="4dp"
        android:text="Tap to open HolyVerso"
        android:textSize="14sp"
        android:textColor="#FFFFFF"
        android:fontFamily="sans-serif-medium"
        android:ellipsize="end"
        android:shadowColor="#80000000"
        android:shadowDx="0"
        android:shadowDy="1"
        android:shadowRadius="2" />

    <!-- Referencia en la parte inferior izquierda -->
    <TextView
        android:id="@+id/widget_reference"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/widget_version"
        android:layout_marginEnd="8dp"
        android:text="Verse of the day"
        android:textSize="12sp"
        android:textColor="#F4D27A"
        android:fontFamily="sans-serif"
        android:shadowColor="#80F4D27A"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="4" />

    <!-- Versión en la esquina inferior derecha -->
    <TextView
        android:id="@+id/widget_version"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:text=""
        android:textSize="9sp"
        android:textColor="#99FFFFFF"
        android:fontFamily="sans-serif"
        android:maxLines="1" />

</RelativeLayout>
